name: launch_pad_app
description: launch pad game

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.0.5 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^1.0.1
  google_fonts: ^3.0.1
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  flutter_app_name:

flutter_app_name:
  "name": "LaunchPad"


flutter:

  uses-material-design: true

